//not sure i need this file as we don't have a front end!

const { ReturnDocument } = require("mongodb")

const handleThoughtSubmit = event => {
    if (!thoughtText || !username) {
        return;
    }
}